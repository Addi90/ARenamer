# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Node / frontend build
node_modules/
dist/

# Built SPA served by the backend (regenerated by `npm run build`)
backend/static/

# PyInstaller packaging (keep the .spec / _bundle.py / build.sh sources; ignore
# the generated workpath + dist output). `dist/` above already covers the bundle.
build/arenamer/
build/smoke/

# OS / editor
.DS_Store
.idea/
.vscode/
